const NotFound = () => {
  return (
    <div
      style={{
        color: "#000",
        fontSize: "20px",
        fontWeight: 700,
        margin: "200px auto",
        textAlign: "center",
      }}
    >
      Страницы не существует!
    </div>
  );
};

export default NotFound;
